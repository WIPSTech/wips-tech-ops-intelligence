/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // This tells the system: "Ignore lint errors during builds"
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
